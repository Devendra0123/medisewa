import assets from "./assets";
import { COLORS, SHADOWS, SIZES, FONTS } from "./theme";
import { ProductData,questionPaper, categories } from "./dummy";

export { assets, COLORS, SHADOWS, SIZES, FONTS, ProductData,questionPaper, categories };
